# === Environment Example ===
# API base (frontend will call `${VITE_API_BASE}/api/...` if not ending with /api )
VITE_API_BASE=http://localhost:8080
# Optional: override proxy target during dev
VITE_PROXY_API=http://localhost:8080
# Session settings
VITE_IDLE_MINUTES=10
VITE_TOKEN_REFRESH_LEEWAY_SEC=90
# Tiles
VITE_TILE_HYBRID=http://localhost:8008/{z}/{x}/{y}.jpg
VITE_TILE_SATELLITE=http://localhost:5005/{z}/{x}/{y}.jpg
VITE_TILE_TMS=true
